# Redirect all requests to the /public folder
<IfModule mod_rewrite.c>
    RewriteEngine On

    # If your Laravel project is in a subfolder, change this to match
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
